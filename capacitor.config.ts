import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto-BovWeight-CR-Movil',
  webDir: 'dist'
};

export default config;
